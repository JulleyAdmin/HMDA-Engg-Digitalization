import React from 'react';
import { Box, Typography, Card } from '@mui/material';
import { HMDAProject } from '../../../../../types/Project';

interface IssueEscalationProps {
  project: HMDAProject;
  compactMode?: boolean;
}

const IssueEscalation: React.FC<IssueEscalationProps> = ({ project, compactMode = false }) => {
  return (
    <Box>
      <Card sx={{ p: 3 }}>
        <Typography variant="h6" gutterBottom>
          Issue Escalation System
        </Typography>
        <Typography variant="body2" color="text.secondary">
          Issue tracking and escalation management will be displayed here.
        </Typography>
      </Card>
    </Box>
  );
};

export default IssueEscalation;